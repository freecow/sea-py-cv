# SeaTable 同步工具配置文件
# 复制此文件为 .env 并填入你的配置信息

# SeaTable API Token (必需)
SEATABLE_TOKEN=your_api_token_here

# SeaTable 服务器地址 (可选，默认为 https://cloud.seatable.cn)
SEATABLE_SERVER_URL=https://cloud.seatable.cn

# 同步配置文件路径 (可选，默认为 config/sync_rules.json)
SEATABLE_CONFIG_FILE=config/sync_rules.json

# 最大并发请求数 (可选，默认为 5)
SEATABLE_MAX_CONCURRENT=5

# 配置说明:
# 1. SEATABLE_TOKEN: 在SeaTable中生成的API Token
# 2. SEATABLE_SERVER_URL: 你的SeaTable服务器地址
#    - 云服务版本: https://cloud.seatable.cn
#    - 私有部署: https://your-domain.com
# 3. SEATABLE_CONFIG_FILE: 同步规则配置文件
#    - 项目统计: config/project_stats_config.json
#    - 工时统计: config/worktime_stats_config.json
# 4. SEATABLE_MAX_CONCURRENT: 并发请求数，建议3-10之间

# 使用方法:
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 编辑 .env 文件，填入正确的配置信息
# 3. 运行同步工具: python run_sync.py